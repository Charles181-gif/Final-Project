// App client-side configuration
// For local development only. Do not commit real keys to public repos.
window.APP_CONFIG = {
  // OpenWeatherMap API key
  // Get one from https://openweathermap.org/api
  OPENWEATHER_API_KEY: ''
};
